import styled from "styled-components";

export const MessageSectionStyled = styled.div`
  padding: 0.5rem 0;
  position: relative;
  width: 100%;
  svg {
    margin-left: 3rem;
    margin-top: 0.75rem;
  }

  p {
    margin-left: 0.5rem;
  }
`;
